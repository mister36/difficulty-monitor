import {
  FindingType,
  FindingSeverity,
  Finding,
  HandleTransaction,
  createTransactionEvent,
  HandleBlock,
} from "forta-agent";
import agent from "./agent";

describe("Difficulty agent", () => {
  let handleBlock: HandleBlock;
});
